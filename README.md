# microsoft-tweet-sentiment-bert
A machine learning project that performs sentiment analysis on Microsoft-related tweets using a fine-tuned BERT model, featuring text preprocessing, SMOTE for data imbalance, and multiclass ROC curve evaluations.
